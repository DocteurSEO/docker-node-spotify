version: "3"
services:
  redis-server:
    image: "redis"
  node-app:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    volumes:
      - ./server:/app
    ports:
      - "4000:4000"
    depends_on:
      - redis-server
